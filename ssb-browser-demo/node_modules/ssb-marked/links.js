

var marked = require('./')

var b = new marked.Renderer()
b.link = function (href, text, title) {
  console.log(href, text, title)
}
var text = 'from the [docs](http://ssbc.github.io/secure-scuttlebutt/) > Since feeds are append-only, replication is simple: request all messages in the feed that are newer than the latest message you know about. how is newer determined? does it use the "sequence number"? could it use the hash of the last message received instead?'
marked(text, {renderer: b})
