import { AddressData as DBData } from 'ssb-conn-db/lib/types';
import { ConnectionData as HubData } from 'ssb-conn-hub/lib/types';
import { StagedData } from 'ssb-conn-staging/lib/types';
export declare type Address = string;
export declare type Poolable = {
    pool: 'db' | 'hub' | 'staging';
};
export declare type Data = Poolable & Partial<DBData> & Partial<HubData> & Partial<StagedData>;
export declare type Peer = [Address, Data];
