import ConnDB = require('ssb-conn-db');
import ConnHub = require('ssb-conn-hub');
import ConnStaging = require('ssb-conn-staging');
import { Peer } from './types';
import * as Time from './queries/time';
import * as Health from './queries/health';
import * as Sorting from './queries/sorting';
declare class ConnQuery {
    private db;
    private hub;
    private staging;
    constructor(db: ConnDB, hub: ConnHub, staging: ConnStaging);
    private _hubEntryToPeer;
    peersAll(): Array<Peer>;
    peersConnected(): Array<Peer>;
    peersConnecting(): Array<Peer>;
    peersInConnection(): Array<Peer>;
    peersConnectable(pool?: 'db' | 'staging' | 'dbAndStaging'): Array<Peer>;
    static passesExpBackoff: typeof Time.passesExpBackoff;
    static passesGroupDebounce: typeof Time.passesGroupDebounce;
    static hasNoAttempts: typeof Health.hasNoAttempts;
    static hasOnlyFailedAttempts: typeof Health.hasOnlyFailedAttempts;
    static hasSuccessfulAttempts: typeof Health.hasSuccessfulAttempts;
    static hasPinged: typeof Health.hasPinged;
    static sortByStateChange: typeof Sorting.sortByStateChange;
}
export = ConnQuery;
